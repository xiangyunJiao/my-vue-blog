# 推荐留空：开发时请求走 Vite 同域（5173/api/...），由 dev server 代理到 Node，避免本机 3000 被其它旧进程占用却仍直连导致异常。
# 若浏览器必须直连后端，填「协议+主机+端口」，不要带 /api，例如 http://127.0.0.1:3000
VITE_API_BASE=

# 代理目标（仅当 VITE_API_BASE 为空时生效），须与 backend 监听端口一致
VITE_DEV_API_PROXY=http://localhost:3000

# 站点名称（可选；不配则使用仓库根目录 config/site.json；管理后台可改库内站点标题）
# VITE_SITE_NAME=
