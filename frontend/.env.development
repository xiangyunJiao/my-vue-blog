# 留空：浏览器请求发到当前页所在源（如 http://localhost:5173/api/...），由 Vite 代理到后端，避免直连错误占用 3000 的旧进程。
# 若需浏览器直连后端调试，可改为 http://127.0.0.1:3000（端口须与 backend 一致，且勿带 /api 后缀）
VITE_API_BASE=

# 站点名称（与 backend/.env 的 SITE_NAME 保持一致）；不配则默认「我的博客」
# VITE_SITE_NAME=我的博客
