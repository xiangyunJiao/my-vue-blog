# 留空：浏览器请求发到当前页所在源（如 http://localhost:5173/api/...），由 Vite 代理到后端，Session Cookie 与页面同域，登录态稳定。
# 若改为直连 http://127.0.0.1:3000，属跨域，浏览器可能不携带 Cookie，侧栏「管理」等登录态会异常。
VITE_API_BASE=

# 站点名称（与 backend/.env 的 SITE_NAME 保持一致）；不配则默认「我的博客」
# VITE_SITE_NAME=小云的随笔集
